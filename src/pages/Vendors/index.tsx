import { Typography } from "@mui/material"
import MainLayout from "../../layout/MainLayout"

export const Vendors = () => {
    return (
        <MainLayout>
            <Typography>
                Vendors
            </Typography>
        </MainLayout>
    )
}